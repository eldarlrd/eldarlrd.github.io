parallel: true
piped: true
commit-msg:
  commands:
    commitlint:
      run: bun commitlint -e -s
pre-commit:
  commands:
    fix:
      run: bun fix && git update-index -g
pre-push:
  commands:
    orphans:
      run: bun orphans
#    cover:
#      run: bun cover
